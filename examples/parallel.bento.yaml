version: "1.0"
type: parallel
name: Parallel Execution
nodes:
    - version: "1.0"
      type: http
      name: Request 1
      parameters:
        method: GET
        url: https://httpbin.org/json
    - version: "1.0"
      type: http
      name: Request 2
      parameters:
        method: GET
        url: https://httpbin.org/headers
