version: "1.0"
type: sequence
name: Sequential Workflow
nodes:
    - version: "1.0"
      type: http
      name: Fetch data
      parameters:
        method: GET
        url: https://httpbin.org/json
    - version: "1.0"
      type: jq
      name: Transform data
      parameters:
        input: '{"test": "value"}'
        query: .
